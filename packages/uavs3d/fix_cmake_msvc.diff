diff --git a/CMakeLists.txt b/CMakeLists.txt
index 5118377..25601c9 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -2,14 +2,17 @@ cmake_minimum_required(VERSION 2.8)

 project(uavs3d)

-aux_source_directory(./test DIR_SRC_TEST)
-set_source_files_properties(${DIR_SRC_TEST} PROPERTIES COMPILE_FLAGS "${CMAKE_C_FLAGS} -std=c99 -O3")
-
 add_subdirectory(./source)

+aux_source_directory(./test DIR_SRC_TEST)
+if(MSVC)
+set_source_files_properties(${DIR_SRC_TEST} PROPERTIES COMPILE_FLAGS "${CMAKE_C_FLAGS} /O2")
+else()
+set_source_files_properties(${DIR_SRC_TEST} PROPERTIES COMPILE_FLAGS "${CMAKE_C_FLAGS} -std=c99 -O3")
+endif()
 add_executable(uavs3dec ${DIR_SRC_TEST})
-
-target_link_libraries(uavs3dec m)
+if(NOT MSVC)
+  target_link_libraries(uavs3dec m)
+endif()
 target_link_libraries(uavs3dec uavs3d)
 #target_link_libraries(uavs3dec dl)
-
diff --git a/source/CMakeLists.txt b/source/CMakeLists.txt
index 4530a63..455d78a 100644
--- a/source/CMakeLists.txt
+++ b/source/CMakeLists.txt
@@ -11,9 +11,15 @@ list(APPEND DIR_UAVS3D_SRC ${DIR_UAVS3D_CORE})

 include_directories("decore")

-set_source_files_properties(${DIR_UAVS3D_SRC} PROPERTIES COMPILE_FLAGS "${CMAKE_C_FLAGS} -fPIC -std=c99 -O3")
-set_source_files_properties(${DIR_X86_SRC} PROPERTIES COMPILE_FLAGS "${CMAKE_C_FLAGS} -fPIC -std=c99 -O3 -msse4.2")
-set_source_files_properties(${DIR_X86_256_SRC} PROPERTIES COMPILE_FLAGS "${CMAKE_C_FLAGS} -fPIC -std=c99 -O3 -mavx2")
+if(MSVC)
+    set_source_files_properties(${DIR_UAVS3D_SRC} PROPERTIES COMPILE_FLAGS "${CMAKE_C_FLAGS} /O2")
+    set_source_files_properties(${DIR_X86_SRC} PROPERTIES COMPILE_FLAGS "${CMAKE_C_FLAGS} /O2 /arch:AVX")
+    set_source_files_properties(${DIR_X86_256_SRC} PROPERTIES COMPILE_FLAGS "${CMAKE_C_FLAGS} /O2 /arch:AVX2")
+else()
+    set_source_files_properties(${DIR_UAVS3D_SRC} PROPERTIES COMPILE_FLAGS "${CMAKE_C_FLAGS} -fPIC -std=c99 -O3")
+    set_source_files_properties(${DIR_X86_SRC} PROPERTIES COMPILE_FLAGS "${CMAKE_C_FLAGS} -fPIC -std=c99 -O3 -msse4.2")
+    set_source_files_properties(${DIR_X86_256_SRC} PROPERTIES COMPILE_FLAGS "${CMAKE_C_FLAGS} -fPIC -std=c99 -O3 -mavx2")
+endif()

 # get version
 set (CONFIG_DIR ${CMAKE_CURRENT_SOURCE_DIR}/..)
@@ -30,6 +36,7 @@ endfunction()
 extract_version_string("${CONFIG_DIR}/version.h" uavs3d_version)
 MESSAGE(STATUS "uavs3d version \t\t: ${uavs3d_version}")

+
 # pkg-config
 find_package(Threads REQUIRED)
 set(prefix "${CMAKE_INSTALL_PREFIX}")
@@ -40,8 +47,8 @@ set(pc_file "${CONFIG_DIR}/${LIBNAME}.pc")
 set(CMAKE_INSTALL_INCLUDE_DIR "${CMAKE_INSTALL_PREFIX}/${includedir}")
 set(CMAKE_INSTALL_LIB_DIR "${CMAKE_INSTALL_PREFIX}/${libdir}")
 set(CMAKE_INSTALL_PKGCONFIG_DIR "${CMAKE_INSTALL_LIB_DIR}/pkgconfig")
-MESSAGE(STATUS "INSTALL_INCLUDE_DIR \t\t: ${CMAKE_INSTALL_INCLUDE_DIR}")
-MESSAGE(STATUS "INSTALL_LIB_DIR \t\t: ${CMAKE_INSTALL_LIB_DIR}")
+MESSAGE(STATUS "INSTALL_INCLUDEDIR \t\t: ${CMAKE_INSTALL_INCLUDE_DIR}")
+MESSAGE(STATUS "INSTALL_LIBDIR \t\t: ${CMAKE_INSTALL_LIB_DIR}")
 MESSAGE(STATUS "INSTALL_PKGCONFIG_DIR\t: ${CMAKE_INSTALL_PKGCONFIG_DIR}")
 # write pkg-config file
 file(WRITE "${pc_file}" "prefix=${prefix}\n")
@@ -53,8 +60,12 @@ file(APPEND "${pc_file}" "Description: AVS3 decoder library \n")
 file(APPEND "${pc_file}" "Version: ${uavs3d_version}\n")
 if(CMAKE_USE_PTHREADS_INIT)
   file(APPEND "${pc_file}" "Libs: -L\${libdir} -l${LIBNAME} -lm -lpthread\n")
+else()
+if(MSVC)
+  file(APPEND "${pc_file}" "Libs: -L\${libdir} -l${LIBNAME}\n")
 else()
   file(APPEND "${pc_file}" "Libs: -L\${libdir} -l${LIBNAME} -lm\n")
+endif()
 file(APPEND "${pc_file}" "Libs.private: \n")
 endif()
 file(APPEND "${pc_file}" "Cflags: -I\${includedir}\n")
@@ -67,13 +78,17 @@ MESSAGE(STATUS "BUILD_SHARED_LIBS \t\t: false")
 endif()
 add_library(${LIBNAME} ${DIR_UAVS3D_SRC} ${DIR_X86_256_SRC} ${DIR_X86_SRC})

+if(MSVC)
+target_compile_definitions(${LIBNAME} PRIVATE UAVS3D_EXPORTS)
+else()
 target_link_libraries(${LIBNAME} m)
+endif()
+
 if(CMAKE_USE_PTHREADS_INIT)
 target_link_libraries(${LIBNAME} pthread)
 endif()

 # install
-install(TARGETS uavs3d LIBRARY DESTINATION ${CMAKE_INSTALL_LIB_DIR} ARCHIVE DESTINATION ${CMAKE_INSTALL_LIB_DIR})
+install(TARGETS uavs3d RUNTIME DESTINATION bin LIBRARY DESTINATION ${CMAKE_INSTALL_LIB_DIR} ARCHIVE DESTINATION ${CMAKE_INSTALL_LIB_DIR})
 install(FILES decoder/uavs3d.h DESTINATION ${CMAKE_INSTALL_INCLUDE_DIR})
 install(FILES ${CONFIG_DIR}/${LIBNAME}.pc DESTINATION ${CMAKE_INSTALL_PKGCONFIG_DIR})
-
