## python モジュールをインストール

```cmd
pip install -r requirements.txt
```
もしここでfletxrのインストールに失敗するならば
```cmd
pip install uv
uv pip install --pre --upgrade
```

## OSLwright を実行する

```cmd
launch-oslwright.bat
```

##
